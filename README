Client controls, 
arrow keys push it in a direction, giving next turn to server

Server controls,
Arrow keys again to select a region of the board. page up and down determine how much it rotates, space actually shifts the board.

Client wins by escaping one of the ports on the side of the maze, server wins from preventing that
